function moranTemp(dataset,geospatial,adjlist){
    console.log("this is awesome");
    var moran_vector = [];
    dataset.forEach(function(feature){
        moran_vector.push(parseFloat(Math.random().toFixed(2)));
    });

    return moran_vector;
}

function testo(phrase){

  //pull values for selected variables

  //import tract weights
$.getJSON("weight_matrix.json", function(weight_matrix) {
  for i in
  // count number of ones in the weight weight_matrix

  // 


  console.log(phrase)
}
